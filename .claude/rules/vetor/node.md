---
paths:
  - "**/*.{ts,tsx,js,jsx,mjs,cjs}"
---

> Gerado por `/vetor` a partir do que foi detectado neste repositório. Editável — o Vetor não sobrescreve sem `--force`.

# Convenções Node/TypeScript (detectadas)

- Gerenciador de pacotes: `npm` (detectado pelo lockfile). Não misture com outros.
- Projeto TypeScript: o `tsconfig.json` da raiz é a fonte das opções de compilação.
